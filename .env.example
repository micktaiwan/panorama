# Panoramix — Production environment
# Copier en .env et remplir les valeurs

# Auth
JWT_SECRET=change-this-to-a-secure-random-string

# CORS (domaine prod, ou * pour tester)
CORS_ORIGIN=*

# AI (remote en prod, pas d'Ollama sur le VPS)
AI_MODE=remote
OPENAI_API_KEY=sk-...
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Notion (optionnel)
NOTION_API_KEY=

# Gmail OAuth2 (optionnel)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://panoramix.example.com/gmail/callback
